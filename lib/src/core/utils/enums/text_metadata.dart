/// Enum for text metadata change
/// It is used to specify which metadata field has changed
enum RTFTextMetadataChangeEnum {
  all,
  color,
  fontWeight,
  fontStyle,
  fontSize,
  alignment,
  fontDecoration,
  fontFeatures;
}
